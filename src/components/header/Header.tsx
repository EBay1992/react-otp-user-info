import Navigation from '../navigation/Navigation';

const Header: React.FC = () => {
  return (
    <header>
      <h1>Portal Challenge</h1>
      <Navigation />
    </header>
  );
};

export default Header;
